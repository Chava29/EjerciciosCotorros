package fors;

import java.util.Scanner;

public class Ejemplo

    /*
    Se divide en tres partes:
    1. Inicialización de la variable (se ejecuto solo una vez al principio del ciclo)
    2. Condición del ciclo (se evalua al comienzo de cada iteración)
    3. Modificación de la variable (se ejecuta al final de cada iteración)
     */


{
    public static void main(String[] args) 
    {
        Scanner scan = new Scanner(System.in);
        int numero;
        for (int contador = 0; contador<10; contador++){
            System.out.println("Estoy en la vuelta N° " + contador);


        }

    }
}
